{
  "mcpServers": {
    "paradigm": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "--package=@a-company/paradigm",
        "-y",
        "paradigm-mcp",
        "${CLAUDE_PROJECT_DIR}"
      ],
      "env": {
        "CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
