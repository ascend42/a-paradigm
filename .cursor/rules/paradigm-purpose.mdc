---
description: Purpose file conventions - .purpose file format and usage
globs: **/.purpose
alwaysApply: false
---
# Purpose Files

Purpose files (`.purpose`) define the context for directories.

## Format

```yaml
# Directory context
description: What this directory contains and why

# Components (# symbol — all code units)
components:
  component-name:
    description: What this component does
    tags: [feature]                   # Classification via tag bank
    gates: [^gate1, ^gate2]          # Required gates
    signals: [!signal1]              # Events emitted
    components: [#Component1]        # Components used
    flows: [$flow1]                  # Flows involved in

# Flows ($ symbol)
flows:
  flow-name:
    description: Multi-step process
    steps:
      - component: #step1
        action: Validate input
      - component: #step2
        action: Process payment
```

## Symbol References

- Reference components: `#component-name`
- Reference gates: `^gate-name`
- Reference signals: `!signal-name`
- Reference flows: `$flow-name`
- Reference aspects: `~aspect-name`
