/**
 * CSS Serializer — Extract clean CSS from Craft.js state
 *
 * Stub for Sprint 0. Sprint 1 will add full CSS extraction
 * from the Craft.js editor state, generating production-ready
 * CSS classes from the visual design.
 */

export function extractCSS(_editorJson: string): string {
  // TODO: Sprint 1 — walk the Craft.js node tree, extract inline
  // styles, generate CSS classes with meaningful selectors
  return '/* CSS export coming in Sprint 1 */';
}
