# v6.0.3 Implementation Plan — Dependency Order

**Status:** partners primitive shipped (commit `49778437`); 8 items remaining.
**Drafted:** 2026-04-25 by main session (Scholar territory; review/refine before execution).
**Locked v6.0.3 scope per recovery checkpoint:** scan-index rename, cross-package imports, PARA 801, companion-lore tool, lore-rejection telemetry, npm deprecate 6.0.0, PLSAT `--delete-sources` docs, wisdom deprecation warnings, CLI portal-loader surfacing, partners primitive (DONE).

**Punted:** Failure Ledger primitive → v6.1; CI drift detector → v6.2.

---

## Phase 1 — Independent / no schema risk (ship together)

These three have zero dependencies on the others and zero schema-breaking surface. Ship as one bundle.

| Item | Surface | Effort | Notes |
|------|---------|--------|-------|
| **npm deprecate 6.0.0** | `npm deprecate @a-company/paradigm@6.0.0 "..."` | <1hr | Single command + verify. Recovery memory notes 6.0.0 had breaking issues; deprecate in favor of 6.0.2+. |
| **PLSAT `--delete-sources` docs** | `docs/guides/university.md` or `packages/paradigm/src/commands/migrate-plsat.ts` help text | ~1hr | Pure docs; flag exists, behavior is documented inconsistently. |
| **Wisdom deprecation warnings** | `packages/paradigm-mcp/src/tools/wisdom.ts` (or wherever deprecated wisdom paths surface) | ~2hrs | Runtime `console.warn` / `log.warn` when deprecated wisdom-tool paths are called. Non-breaking. |

**Phase 1 risk:** none. **Why first:** clears low-effort items; no dependency on later work.

---

## Phase 2 — Schema/format work (depends on Phase 1 clean)

| Item | Surface | Effort | Dependencies |
|------|---------|--------|--------------|
| **scan-index rename** | `scan-index.json` → `probe-index.json` (and any related artifact filenames). Per CHANGELOG line 24 the spec rename was done in v6.0.2 but the index-file rename is the remaining piece. | ~3hrs | Audit all consumers of `scan-index.json`; add migration path that reads old name, writes new name. |
| **companion-lore tool** | New MCP tool: `paradigm_companion_lore_record` (or similar). Per memory the companion-lore pattern was the v6.0 D3 pattern — the tool surfaces it. | ~4hrs | Schema design; type definitions in `paradigm-mcp/src/types/`; tool handler; tests. |
| **lore-rejection telemetry** | Emit telemetry event when `paradigm_lore_record` rejects a record (e.g., type='decision' rejection). Type system already has the rejection envelope; this just adds emission to a telemetry stream. | ~2hrs | Hook into existing rejection envelope at the emission point. |

**Phase 2 risk:** scan-index rename has highest blast radius — every project's existing `scan-index.json` needs migration. Add a regression test that round-trips through the rename. **Why before Phase 3:** companion-lore tool needs lore-rejection telemetry to validate end-to-end; both feed Phase 3's CLI surfacing.

---

## Phase 3 — Architectural / cross-cutting (depends on Phase 2)

| Item | Surface | Effort | Dependencies |
|------|---------|--------|--------------|
| **Cross-package imports cleanup** | `paradigm` CLI imports `paradigm-mcp` source files via relative paths in many places (per CHANGELOG line 24 — this is why `tsc --noEmit` is currently skipped). Refactor to import via package boundary. | ~1-2 days | Will surface circular-dep risks. Audit before starting. Likely affects: `commands/agent/index.ts` (which already duplicates AgentProfile rather than importing — done deliberately), team types, MCP tool re-exports. |
| **PARA 801** | UNKNOWN scope without the issue tracked. Action: locate PARA 801 (Linear? GitHub? memory entry?) before estimating. | ? | Research first. |
| **CLI portal-loader surfacing** | Surface portal-loader discrimination (the v6.0 portal v1 vs v2 distinction) in `paradigm doctor` / `paradigm sync` output. | ~3hrs | Phase 2 lore-rejection telemetry pattern can be reused for visibility format. |

**Phase 3 risk:** cross-package imports refactor is the largest single unit of work in v6.0.3. May warrant its own PR. **Why last:** depends on Phase 2 stabilizing the schema surface; refactoring imports while schema is in flux doubles the merge conflict surface.

---

## Recommended commit/release shape

- **6.0.3-rc.1:** Phase 1 bundle (3 small items)
- **6.0.3-rc.2:** Phase 2 bundle (3 schema items)
- **6.0.3 final:** Phase 3 bundle (3 architectural items, including PARA 801 if scoped by then)

OR — single 6.0.3 release with all 8 items if Phase 3 effort comes in under estimate.

Per release habit (memory): each phase lands as a separate commit with CHANGELOG entry; bump versions only at the final 6.0.3 cut.

---

## Open questions

1. **PARA 801 scope** — needs research before it can be ordered. Currently guessed in Phase 3 but might collapse to Phase 1 if it's small.
2. **Cross-package imports refactor** — does it change public API surface? If yes, it's a 6.1 candidate, not 6.0.3.
3. **Phase 3 sequencing** — should CLI portal-loader surfacing wait for cross-package imports cleanup, or can it ship parallel?

Resolve before kicking off Phase 1.
