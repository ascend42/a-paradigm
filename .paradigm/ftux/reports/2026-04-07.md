# FTUX Friction Report — 2026-04-07

**Agent:** Nora (FTUX)
**Simulated Persona:** A developer with no prior Paradigm exposure; found the project on GitHub/npm; wants to install it and get it working with Claude Code.
**Surfaces Read (in order):**
1. README.md
2. docs/guides/quick-start.md
3. docs/guides/mcp-setup.md
4. plugins/paradigm-cursor/README.md

---

## Surface Examined

- README.md — landing page, first thing a GitHub visitor sees
- docs/guides/quick-start.md — linked from README as "Step-by-step setup walkthrough"
- docs/guides/mcp-setup.md — linked from README as "MCP Setup — AI client integration guide"
- plugins/paradigm-cursor/README.md — linked from README plugin table

---

## Task Attempted

**Goal:** Install Paradigm, connect it to Claude Code, and verify the MCP tools are working.

A real developer landing on this GitHub repo would:
1. Read the README to understand what Paradigm is and how to install it
2. Follow the Quick Start guide
3. Follow the MCP setup guide to get Claude Code working
4. Confirm their Claude Code session has access to the tools

---

## Step-by-Step Walkthrough

### Step 1: Read the README to understand the product

- **What I did:** Read the README from top to bottom.
- **Outcome:** Success for understanding the concept; blocked on installation.
- **Friction:** contradictory — **high**
- **Note:** The README has two contradictory installation paths with conflicting claims.

  The "Quick Install (Recommended)" section says:
  > `curl -fsSL https://a-company.org/paradigm/install.sh | bash`

  But immediately below, a separate note says:
  > "Note: Paradigm is not yet published to npm. Install from source using the methods above. npm registry publishing is planned."

  As a first-time user I don't know which is true. The curl installer presumably fetches from the internet — does it work? Is `https://a-company.org/paradigm/install.sh` live? The note about "not yet published to npm" appears under "Manual Install" but bleeds into my mental model of the curl install too. I cannot tell if the recommended install path actually works without trying it.

---

### Step 2: Try "Quick Install (Recommended)" — curl method

- **What I did:** Tried to follow `curl -fsSL https://a-company.org/paradigm/install.sh | bash`
- **Outcome:** Blocked — I cannot verify this URL is live from the docs alone.
- **Friction:** missing_coverage — **critical**
- **Note:** No docs confirm the install script URL is live, what it does, or whether it is safe to pipe to bash. The README says "This clones to `~/.paradigm-cli/`, builds, and installs the `paradigm` and `paradigm-mcp` CLIs globally." That's helpful context but the URL's availability is not verifiable from reading alone. For a security-conscious developer, a curl-pipe-to-bash install requires trusting that URL and knowing it actually exists. The README offers no verification step (e.g., "confirm with `paradigm --version` shows X.Y.Z"). Seeing "npm registry publishing is planned" raises the question: is this curl installer the real supported path, or a placeholder?

---

### Step 3: Fall back to Manual Install (from README)

- **What I did:** Followed the "Manual Install" steps:
  ```
  git clone https://github.com/ascend42/a-paradigm.git ~/.paradigm-cli
  cd ~/.paradigm-cli
  npm install && npm run build
  cd packages/paradigm && npm install -g . && cd ../..
  cd packages/paradigm-mcp && npm install -g . && cd ../..
  paradigm --version
  ```
- **Outcome:** Confused at step 3.
- **Friction:** missing_coverage — **high**
- **Note:** The README says to install both `packages/paradigm` AND `packages/paradigm-mcp` globally. But the Quick Start guide (which I click to next) says only:
  ```
  npm link @a-company/paradigm
  ```
  No mention of `paradigm-mcp` in the Quick Start installation. I don't know if `paradigm-mcp` needs to be installed separately for MCP to work, or if `paradigm mcp setup` handles it. The relationship between the `paradigm` CLI and `paradigm-mcp` binary is never explained from a user perspective.

---

### Step 4: Open docs/guides/quick-start.md — "Super Quick Install"

- **What I did:** Followed the link from README to quick-start.md.
- **Outcome:** Confused.
- **Friction:** broken_flow — **critical**
- **Note:** The "Super Quick Install" one-liner is:
  ```
  git clone https://github.com/ascend42/a-paradigm.git /tmp/paradigm-temp && cd /tmp/paradigm-temp && npm install && npm run build && npm link @a-company/paradigm && cd - && paradigm init --quick && ...
  ```
  This uses `npm link @a-company/paradigm` — which links the package from the cloned repo into global npm, but then cleans up with `rm -rf /tmp/paradigm-temp`. The README's own note says "The source directory at `~/.paradigm-cli/` must be kept — the global CLIs symlink to it." The Quick Start one-liner clones to `/tmp/paradigm-temp` and then explicitly says to delete it. If `npm link` creates a symlink, deleting the source directory would break the linked CLI. This is a direct contradiction of the README's own warning. A first-time user following this exactly would end up with a broken install.

---

### Step 5: Attempt `paradigm init --quick`

- **What I did:** Following the step-by-step Quick Start after installing the CLI.
- **Outcome:** Confused about what `paradigm init --quick` does vs. `paradigm init`.
- **Friction:** undefined_term — **medium**
- **Note:** The Quick Start and README both use `--quick` without explaining what it skips relative to plain `paradigm init`. The README also lists `paradigm init --migrate` as a separate variant. No doc explains what "quick" mode does vs. interactive mode. A first-time user doesn't know if `--quick` is appropriate for their case or if interactive mode would configure something important.

---

### Step 6: Attempt `paradigm mcp setup --client all`

- **What I did:** Read the README's MCP section and the full mcp-setup.md guide.
- **Outcome:** Confused — Claude Code is conspicuously absent.
- **Friction:** missing_coverage — **critical**
- **Note:** This is the most significant finding. The target user is explicitly a developer who wants to "get it working with Claude Code." However:

  - The "Supported Clients" table in mcp-setup.md lists: **Cursor, Claude Desktop, Continue (VS Code), Cline (VS Code)**. Claude Code is **not listed**.
  - The manual setup section covers: Cursor, Claude Desktop, Continue. Claude Code manual config is **not present**.
  - The "In Your AI Client" verification section in quick-start.md includes "Cursor", "Claude Code", and "Claude Desktop" — so Claude Code is mentioned in the verification step, but the guide never explains *how* to configure MCP for Claude Code before that.
  - The README MCP section shows a JSON snippet with `"command": "paradigm-mcp"` — but this uses the binary name, not `npx`. Users who only installed via `npm link` from source may not have `paradigm-mcp` on their PATH.
  - The quick-start.md verification step says: **"Try: 'What paradigm tools do you have access to?'"** — this implies the user already has MCP working in Claude Code, but the docs never told them how to get there.

---

### Step 7: Try to understand "Claude Code Plugin" install path

- **What I did:** Found the plugin section of README: `/plugin marketplace add ascend42/a-paradigm`
- **Outcome:** Blocked — unclear how this relates to MCP, or if it replaces `paradigm mcp setup`.
- **Friction:** assumed_context — **high**
- **Note:** The README mentions a Claude Code plugin at `plugins/paradigm/` distributed via `/plugin marketplace add ascend42/a-paradigm`. This is presented alongside `paradigm mcp setup --client all`. A first-time Claude Code user does not know:
  - Whether they should install the plugin OR run `mcp setup`, or both
  - Whether the plugin automatically configures MCP or if they still need `paradigm mcp setup --client claude-code`
  - What "Plugin vs per-project setup" means in practice for Claude Code (the table only appears in the Cursor plugin README)

The README says "Most teams use both: plugin for enforcement, `paradigm shift` for project-specific context" — but this doesn't clarify the MCP question at all.

---

### Step 8: Read plugins/paradigm-cursor/README.md for comparison

- **What I did:** Followed the link in the README plugin table to the Cursor plugin README.
- **Outcome:** Partially successful for Cursor users; highlights the gap for Claude Code.
- **Friction:** missing_coverage — **medium**
- **Note:** The Cursor plugin README is clear and thorough — it explains hooks, enforcement flow, plugin vs. per-project, requirements. There is no equivalent README for the Claude Code plugin at `plugins/paradigm/`. The README table points to `plugins/paradigm/` for the Claude Code plugin, but there is no separate README or doc linked that explains what the Claude Code plugin provides or how to verify it's working.

---

### Step 9: Check "Next Steps" in mcp-setup.md

- **What I did:** Read the bottom of mcp-setup.md.
- **Outcome:** Confused.
- **Friction:** broken_flow — **low**
- **Note:** The final "Next Steps" bullet says: "Try the TaskFlow Tutorial — Build a project step-by-step with MCP integration." There is no link. There is no indication of where to find the TaskFlow Tutorial. The second bullet says "Join the Community" with no link or platform named. These are dead ends.

---

### Step 10: Look for error resolution if something goes wrong

- **What I did:** Read the Troubleshooting section of mcp-setup.md.
- **Outcome:** Partially useful.
- **Friction:** assumed_context — **medium**
- **Note:** The troubleshooting section references `paradigm mcp setup --client cursor --force` and instructs users to "Restart the AI client completely." For Claude Code users, there's no equivalent troubleshooting guidance. The troubleshooting section focuses on Cursor and Claude Desktop. A Claude Code user hitting a problem has no troubleshooting path in any of these docs.

---

## Friction Summary

| Step | Type | Severity | Description |
|------|------|----------|-------------|
| 1 | contradictory | high | README has curl "Recommended" install but then says "not yet published to npm" — unclear if install.sh is live or a placeholder |
| 2 | missing_coverage | critical | No confirmation the curl install URL is live; no expected output after install to confirm success |
| 3 | missing_coverage | high | `paradigm-mcp` binary install missing from Quick Start; unclear if it's needed or auto-handled |
| 4 | broken_flow | critical | Quick Start one-liner deletes `/tmp/paradigm-temp` after `npm link`, but README warns the symlink source must be kept — contradictory instructions produce a broken install |
| 5 | undefined_term | medium | `--quick` flag undocumented; user doesn't know what interactive `paradigm init` configures differently |
| 6 | missing_coverage | critical | Claude Code is absent from the MCP Supported Clients table and manual setup section, yet is mentioned as a verification target |
| 7 | assumed_context | high | Claude Code plugin vs. `paradigm mcp setup` relationship not explained; user doesn't know if plugin replaces or supplements MCP setup |
| 8 | missing_coverage | medium | No README or doc for the Claude Code plugin equivalent to the Cursor plugin README |
| 9 | broken_flow | low | "TaskFlow Tutorial" and "Community" links in mcp-setup.md Next Steps lead nowhere |
| 10 | assumed_context | medium | Troubleshooting in mcp-setup.md covers Cursor/Claude Desktop only; no Claude Code troubleshooting path |

---

## Verdict

**Overall readiness: needs-work**

The docs have a significant structural gap: Claude Code is the primary advertised AI tool (README headline, badge, MCP tool names all reference it), but it is missing from the Supported Clients table in mcp-setup.md, absent from manual setup instructions, and has no dedicated plugin README. A developer specifically trying to use Paradigm with Claude Code will be able to understand the product but will not be able to successfully complete MCP setup using only the user-facing documentation. The secondary critical issue is the contradictory installation paths — the Quick Start one-liner is functionally broken (deletes the source the symlink depends on).
